import React from 'react'
import { useContext } from 'react'
import { Shopcontext } from '../contexts/ShopContext'

function LatestCollections() {
   
  const {products} =useContext(ShopContext);

  return (
    <div>
      
    </div>
  )
}

export default LatestColle
   
const {products} =useContext(Shopcontext)
ctions
